<div class="profile_image">
  <div class="profile_mask">
    <div class="content-title">
      <h1>プロフィール</h1><br><br>
      <table class="table table-border" id="user-profile">
        <tr>
          <th class="profile-title" width="50%">ユ ー ザ ー 名</th>
          <th><%= @user.name %></th>
        </tr>
        <tr>
          <th class="profile-title">メ ー ル ア ド レ ス</th>
          <th><%= @user.email %></th>
        </tr>
        <tr>
          <th class="profile-title">現在の認可申請件数(担当分)</th>
          <th><%= @user.application_documents.count %> 件</th>
        </tr>
        <tr>
          <th class="profile-title">＜件数内訳＞</th>
          <td></td>
        </tr>
        <tr>
          <th class="profile-title"><span>（未処理・処理中の件数）<span></th>
          <th><span>（<%= @user.application_documents.processing_incomplete.count %> 件）</span></th>
        </tr>
        <tr>
          <th class="profile-title"> （処理済の件数）</th>
          <th>（<%= @user.application_documents.processing_complete.count %> 件）</th>
        </tr>
      </table><br><br><br>
      <div class="sa"></div>
      <% if current_user.try(:admin?) %>
      <h3><b>【各ユーザーの申請処理件数一覧(管理者用)】</b></h3><br>
      <table class="table table-striped" id="user-profile">
        <tr class="table-danger">
          <th>ユーザー名</th>
          <th>認可申請件数</th>
        </tr>
        <% @users.each do |user| %>
        <tr class="task-count">
          <td><%= user.name %></td>
          <td><%= user.application_documents.count %> 件</td>
        </tr>
        <% end %>
      </table><br>
      <%= paginate @users, class: "page" %>
      <% end %>
    </div><br><br>
    <%= link_to '戻 る', kindergartens_path, class: "link" %><br><br>
  </div>
</div>
